{
  "name": "vanilla-ts",
  "version": "0.0.1",
  "scripts": {
    "build": "rm -rf ./js && npx tsc",
    "start": "busybox httpd -f -h . -p 0.0.0.0:8080 -v"
  },
  "dependencies": {},
  "devDependencies": {
    "npm-check-updates": "^12.0.0",
    "typescript": "^4.4.4"
  }
}
