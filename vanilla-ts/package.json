{
  "name": "vanilla-ts",
  "version": "0.0.1",
  "scripts": {
    "build": "rm -rf ./build/* && mkdir -p ./build/js && cp -r ./src/* ./build && rm -rf ./build/ts && npx tsc",
    "clean": "rm -rf ./build",
    "start": "busybox httpd -f -h ./build -p 0.0.0.0:8080 -v"
  },
  "dependencies": {},
  "devDependencies": {
    "npm-check-updates": "^13.0.3",
    "typescript": "^4.7.2"
  }
}
